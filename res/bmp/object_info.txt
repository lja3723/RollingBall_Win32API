﻿# 이렇게 # 뒤에 오는 문장은 주석입니다.

# 이 파일은 오브젝트에 대한 정보를 표현합니다.
# 이 파일은 res\bmp\ 폴더에 위치해야 합니다.
# 이 파일은 object_info.txt 라는 이름이어야 합니다.

<object_info.txt>

# 프로그램에서 사용될 오브젝트의 개수입니다.
object_count=2;

#오브젝트에 대한 정보의 시작임을 알립니다.
<object>

# 오브젝트 이름입니다.
name=ball;

# 텍스쳐의 개수를 나타냅니다.
texture_count=1;

# 텍스쳐의 이름을 나타냅니다.
texture_name=iron1;

# 텍스쳐의 크기 종류를 나타냅니다.
texture_size_count=4;

# 텍스쳐 크기 값을 나타냅니다.
#texture_size_val=32,64,128,256;

#오브젝트가 마스크 이미지를 가지는지 나타냅니다.(TRUE / FALSE)
has_mask=TRUE;

# 오브젝트에 대한 정보가 끝났음을 알립니다.
</object>


<object>
name=floor;
texture_count=1;
texture_name=wood1;
texture_size_count=1;
texture_size_val=256;
has_mask=FALSE;
</object>


</object_info.txt>